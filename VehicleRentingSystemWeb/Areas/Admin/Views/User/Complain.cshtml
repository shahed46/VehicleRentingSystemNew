@model IEnumerable<Complain>
<div class="container p-3 mt-5">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Complain List</h2>
        </div>
        @*<div class="col-6 text-end">
            <a asp-controller="Category" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create new Category
            </a>
        </div>*@
    </div>

</div>
</br> </br>
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Complainer Name
            </th>
            <th>
                Driver Name
            </th>
            <th>
                Complain Description
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="20%">
                    @obj.ApplicationUser.Name
                </td>
                <td width="20%">
                    <a asp-area="User" asp-controller="Profile" asp-action="Index" asp-route-userId="@obj.DriverUserId" style="text-decoration:none">  @obj.DriverName</a>
                </td>
                <td width="60%">
                    @obj.ComplainDescription
                </td>
                @*<td>
                    <div class="w-75  btn-group" role="group">
                        <a asp-controller="Category" asp-action="Edit" asp-route-id="@obj.Id"> <i class="bi bi-pencil-square"></i>Edit</a>

                        <a asp-controller="Category" asp-action="Delete" asp-route-id="@obj.Id"> <i class="bi bi-trash"></i>Delete</a>
                    </div>
                </td>*@

            </tr>
        }
    </tbody>
</table>